# Applications

- iTerm2
- Xcode

# Command-line interface

```
zsh
brew

sudo gem intall cocoapods
pod --version

nvm --version

nvm install
npm --version

npm install -g yarn
yarn --version

npm install -g ios-deploy
ios-deploy --version
```
